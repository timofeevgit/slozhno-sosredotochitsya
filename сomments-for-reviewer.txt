GULP:
1. - Соответствие страницы с макетом сверялось только по файлам в папке src, т.к. команды сборки build и dev, указанные в package.json, по-прежнему не отрабатывают, как ожидалось, выпадает ошибка
/// Папка /dist прописана в .gitignore (по рекомендациям Практикума), поэтому в репозитрий уходит только /src
/// У меня выполняются и корректно работают команды сборки dev и build, никаких ошибок не возникает. Проект корректно открывается на сервере, а также сборка в /dist осуществляется как задумано. Я не могу понять в чем ошибка. Пожалуйста, направьте меня.
  Команда dev выполняется, если прописать 'gulp' в теримнале.
  Команда build выполняется, если прописать 'gulp build' в терминале.
P.S. папку node_modules вытащил из .gitignore, может быть проблема была в этом.
/// Командну конкатенации всех стилей в один bundle.css я закоментировал, так как в данный момент она не нужна. Из вёрстки удалил лишний link.

HTML:
1. — в файле есть ошибки W3C валидации/
/// Валидатор по вашей ссылке не выдаёт ни одной ошибки и ни одного предупреждения. Только зелёные "info". На всякий случай исправил и их.
